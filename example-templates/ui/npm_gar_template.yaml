name: Build and Deploy

on:
  pull_request_review:
    types: [ submitted ]
    branches:
      - main
      - development
  push:
    tags:
      - '*'
    branches:
      - main
      - development

jobs:
  test:
    uses: zopsmart/zs-workflows/.github/workflows/ui_npm.yaml@v0.3.8
    with:
      APP_NAME: ${{ matrix.app_name }}
      NODE_VERSION: '18.x'
      TEST_COMMAND: |
        CI=false npm run test:coverage
      LINTER_CHECKS: true
      PRETTIER_CHECKS: true
      ENABLE_TESTS: false
      DEPENDENCIES_FLAG: --legacy-peer-deps //not required
    secrets:
      PAT: ${{ secrets.PAT }}

  dockerize_deploy:
    needs: [ test ]
    strategy:
      fail-fast: false
      matrix:
        include:
          - app_name: ${{ vars.STAGE_APP_NAME }}
            build_commands: "<build command>"
            DOCKER_FILE_PATH: "Dockerfile" #path of dockerfile including the name of the file
            namespace: ${{ vars.STAGE_NAMESPACE }}
            type: "deployment"

    uses: zopsmart/zs-workflows/.github/workflows/npm_stage_deploy.yaml@v0.3.8
    with:
      APP_NAME: ${{ matrix.app_name }}
      NODE_VERSION: '18.x'
      BUILD_COMMAND: ${{ matrix.build_commands }}
      DOCKER_FILE_PATH: ${{ matrix.DOCKER_FILE_PATH }}
      NAMESPACE: ${{ matrix.namespace }}
      CLUSTER_NAME: ${{ vars.STAGE_CLUSTER_NAME }} // Skip this input, if only dockerize is needed
      CLUSTER_PROJECT: ${{ vars.STAGE_CLUSTER_PROJECT }}
      CLUSTER_REGION: cluster region
      IMAGE_REGISTRY: us-central1-docker.pkg.dev
      GAR_PROJECT: ${{ vars.STAGE_GAR_PROJECT }}
      GAR_REGISTRY: ${{ vars.STAGE_GAR_REGISTRY }}
      TYPE: ${{ matrix.type }}
      SHA: ${{ needs.test.outputs.SHA }}
    secrets:
      PAT: ${{ secrets.STAGE_PAT }}
      NPM_TOKEN: ${{ secrets.PRIVATE_GITHUB_PACKAGE_PAT }} #not required
      GAR_KEY: ${{ secrets.STAGE_GAR_KEY }}

  prod_deployment:
    strategy:
      fail-fast: false
      matrix:
        include:
          - app_name: ${{ vars.STAGE_APP_NAME }}
            namespace: ${{ vars.PROD_NAMESPACE }}
            type: "deployment"
    uses: zopsmart/zs-workflows/.github/workflows/prod_deploy.yaml@v0.3.8
    with:
      APP_NAME: ${{ matrix.app_name }}
      NAMESPACE: ${{ matrix.namespace }}
      IMAGE_REGISTRY: us-central1-docker.pkg.dev
      GAR_PROJECT: ${{ vars.PROD_GAR_PROJECT }}
      GAR_REGISTRY: ${{ vars.PROD_GAR_REGISTRY }}
      CLUSTER_NAME: ${{ vars.PROD_CLUSTER_NAME }}
      CLUSTER_PROJECT: ${{ vars.PROD_CLUSTER_PROJECT }}
      CLUSTER_REGION: cluster region
      TYPE: ${{ matrix.type }}

    secrets:
      GAR_KEY: ${{ secrets.STAGE_GAR_KEY }}
