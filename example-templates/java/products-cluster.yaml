name: Build and Deploy Service

on:
  pull_request_review:
    types: [ submitted ]
    branches:
      - main
  push:
    tags:
      - '*'
    branches:
      - main

jobs:
  build:
    uses: zopsmart/zs-workflows/.github/workflows/java_build.yaml@v0.3.6
    secrets:
      GAR_SECRET: ${{ secrets.GAR_KEY }}

  stage_deployment:
    needs: [ build ]
    strategy:
      fail-fast: false
      matrix:
        include:
          - DOCKER_FILE_PATH: "Dockerfile"
            app_name: "training-api"
            namespace: "training-stage"
            type: "deployment"


    with:
      IMAGE_REGISTRY: us-central1-docker.pkg.dev  #GAR registry
      username: _json_key
      PROJECT_NAME: "zs-products"
      DOCKER_FILE_PATH: ${{ matrix.DOCKER_FILE_PATH }}
      BUILD_ARGUMENTS: |
        VERSION=edge
        APP_NAME=main
      APP_NAME: ${{ matrix.app_name }}
      NAMESPACE: ${{ matrix.namespace }}
      CLUSTER_NAME: products-cluster
      CLUSTER_PROJECT: zs-products
      GCR_PROJECT: zs-products/zopping
      SHA: ${{ github.sha }}
      TYPE: ${{ matrix.type }}
      ARTIFACT_PATH: build/libs
      ARTIFACT_NAME: artifacts
      ENV_FILE_PATH: ./configs/.stage.env
    secrets:
      GCR_KEY: ${{ secrets.GCR_KEY }}
      DEPLOY_KEY: ${{ secrets.DEPLOY_KEY}}

    uses: zopsmart/zs-workflows/.github/workflows/gcr_stage_deploy.yaml@v0.3.6

  prod_deployment:
    strategy:
      fail-fast: false
      matrix:
        include:
          - app_name: "training-api"
            namespace: "training"
            type: "deployment"


    with:
      IMAGE_REGISTRY: us-central1-docker.pkg.dev  #GAR registry
      username: _json_key
      PROJECT_NAME: "zs-products"
      APP_NAME: ${{ matrix.app_name }}
      NAMESPACE: ${{ matrix.namespace }}
      CLUSTER_NAME: products-cluster
      CLUSTER_PROJECT: zs-products
      GCR_PROJECT: zs-products/zopping
      TYPE: ${{ matrix.type }}
      ARTIFACT_PATH: build/libs
      ARTIFACT_NAME: artifacts
      ENV_FILE_PATH: ./configs/.prod.env
    secrets:
      GCR_KEY: ${{ secrets.GCR_KEY }}
      DEPLOY_KEY: ${{ secrets.DEPLOY_KEY}}

    uses: zopsmart/zs-workflows/.github/workflows/gcr_prod_deploy.yaml@v0.3.5
